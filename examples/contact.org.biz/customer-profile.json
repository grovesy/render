{
    "$id": "data://contact.org.biz/model/1/customer-profile",
    "title": "CustomerProfile",
    "type": "object",
    "description": "A customer profile linking core contact information, addresses, and communication channels.",
    "properties": {
        "customerId": {
            "type": "string",
            "description": "The unique identifier for the customer."
        },
        "contactId": {
            "$ref": "data://contact.org.biz/model/1/contact#/properties/contactId"
        },
        "primaryAddressId": {
            "type": "string",
            "description": "Identifier of the customer's primary address."
        },
        "status": {
            "type": "string",
            "enum": ["active", "inactive", "prospect", "closed"],
            "description": "Lifecycle status of the customer."
        },
        "createdAt": {
            "type": "string",
            "description": "Timestamp indicating when the customer profile was created."
        },
        "updatedAt": {
            "type": "string",
            "description": "Timestamp indicating when the customer profile was last updated."
        }
    }
}
