{
  "name": "schema-graph-viewer",
  "version": "1.0.0",
  "description": "Visualize JSON schema domains and relationships in a modern browser graph",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
